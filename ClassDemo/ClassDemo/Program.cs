namespace ClassDemo;

// Public is an "Access Modifier" Default is internal
public class Program
{
    // Public is an "Access Modifier". Default is private
    public static void Main(string[] args)
    {
        // F12 to Go-To Method, Ctrl - to "go back"
        Console.WriteLine("Hello, World!");
        "1".ToString();
        Console.WriteLine(1.GetType());
    }
}
